import React from 'react'
import { SamuraiUIFlexProps } from '../../types'
import { StyledFlex } from './styled'

function Flex(props: SamuraiUIFlexProps) {
  return <StyledFlex {...props} />
}

export default Flex
